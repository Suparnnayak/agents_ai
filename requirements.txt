# =============================================================================
# Hospital Forecast API — Slim Dependencies (Vercel / Serverless)
# =============================================================================
# This file contains ONLY what the FastAPI application needs at runtime.
# Vercel auto-installs from this file.  ~50 MB total — well under 500 MB.
#
# For ML scripts (training, forecasting, retraining), use:
#   pip install -r requirements-ml.txt
# =============================================================================

# ---- API Framework ----
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# ---- Database ----
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9

# ---- Auth ----
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# ---- Pydantic / Validation ----
pydantic>=2.7.0
pydantic-settings>=2.2.1
email-validator>=2.0.0

# ---- Utilities ----
python-dotenv>=1.0.0
python-dateutil>=2.8.0
requests>=2.31.0
