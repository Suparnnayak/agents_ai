services:
  - type: web
    name: hospital-forecast-api
    env: python
    buildCommand: cd agents_ai && pip install -r requirements.txt
    startCommand: cd agents_ai && uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
    healthCheckPath: /health
    plan: free  # or 'starter' for paid tier

